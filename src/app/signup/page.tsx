import Link from 'next/link'
import { signup } from '../auth/actions'
import { AuthShell, Field } from '../login/page'

export default async function SignupPage({ searchParams }: { searchParams: Promise<{ error?: string }> }) { const { error } = await searchParams; return <AuthShell title="Create your workspace" subtitle="Start free. Upgrade only when ContentForge earns its place in your workflow."><form action={signup} className="space-y-4"><Field name="name" label="Your name" /><Field name="email" label="Email" type="email" /><Field name="password" label="Password" type="password" />{error && <p role="alert" className="rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</p>}<button className="w-full rounded-lg bg-orange-500 px-4 py-3 font-semibold text-white hover:bg-orange-600">Create free account</button></form><p className="mt-4 text-xs leading-5 text-neutral-500">By continuing, you agree to the <Link href="/terms" className="underline">Terms</Link> and <Link href="/privacy" className="underline">Privacy Policy</Link>.</p><p className="mt-5 text-center text-sm text-neutral-500">Already have an account? <Link href="/login" className="font-medium text-orange-600">Log in</Link></p></AuthShell> }
