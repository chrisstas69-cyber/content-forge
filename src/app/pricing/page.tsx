import Link from 'next/link'
import { Check, Sparkles } from 'lucide-react'
import { CheckoutButton } from '@/components/checkout-button'

const tiers = [{ name: 'Free', price: '$0', desc: 'Try the core workflow', features: ['5 monthly credits','Ideas and scripts','Manual exports'] }, { name: 'Creator', price: '$19', desc: 'For consistent creators', features: ['50 monthly credits','AI images and thumbnails','Scheduling and analytics'], plan: 'creator' as const }, { name: 'Pro', price: '$49', desc: 'For serious production', features: ['200 monthly credits','Priority generation','Advanced analytics'], plan: 'pro' as const }]
export default function PricingPage() { return <main className="min-h-screen bg-neutral-950 px-6 py-16 text-white"><div className="mx-auto max-w-6xl"><Link href="/" className="flex items-center gap-2 font-bold"><Sparkles className="size-5 text-orange-500" /> ContentForge</Link><div className="mt-16 text-center"><h1 className="text-4xl font-black sm:text-5xl">Simple pricing that grows with your output.</h1><p className="mt-4 text-neutral-400">Start free. Upgrade when ContentForge saves you real time.</p></div><div className="mt-12 grid gap-5 md:grid-cols-3">{tiers.map(tier => <div key={tier.name} className={`rounded-2xl border p-6 ${tier.name === 'Creator' ? 'border-orange-500 bg-orange-500/5' : 'border-white/10 bg-white/[0.03]'}`}><h2 className="text-xl font-bold">{tier.name}</h2><p className="mt-2 text-sm text-neutral-400">{tier.desc}</p><p className="mt-6 text-4xl font-black">{tier.price}<span className="text-sm font-normal text-neutral-500">{tier.price !== '$0' && '/month'}</span></p><ul className="my-7 space-y-3">{tier.features.map(f => <li key={f} className="flex gap-2 text-sm text-neutral-300"><Check className="size-4 text-emerald-400" />{f}</li>)}</ul>{tier.plan ? <CheckoutButton plan={tier.plan}>Choose {tier.name}</CheckoutButton> : <Link href="/signup" className="block rounded-lg border border-white/20 px-4 py-3 text-center font-semibold hover:bg-white/5">Start free</Link>}</div>)}</div></div></main> }
